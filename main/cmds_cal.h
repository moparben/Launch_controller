/* cmds_cal.h - Console registration for calibration control */

#pragma once

#include <stdbool.h>

/* Register 'cal_disable' console command (developer console only) */
void register_cal_console_cmd(void);
